schema-version: 1.0
description: Build id3lib 3.8.3 for ID3 metadata manipulation.
require: ../../build-system
builder:
  - { origin: ../package-template, alias: tools }
  - { origin: ../blfs-sources, alias: sources }
  - { origin: ../final-system, alias: system }
  - { origin: ../package-autoconf, alias: autoconf }
  - { origin: ../package-automake, alias: automake }
  - { origin: ../package-libtool, alias: libtool }
  - { origin: ../package-zlib, alias: zlib }
var:
  xbee: { version: "3.8.3-1" }
  lfs: { jobs: 4 }
  package:
    name: id3lib
    version: "3.8.3"
    source: id3lib-3.8.3.tar.gz
    runtime-dependencies: zlib
build:
  - shell:
      cmd:
        - >-
          bash "/opt/xbee-lfs-native/build-xbpkg.sh"
          "/opt/xbee-lfs-native/sources" "/opt/xbee-lfs-native/final-rootfs.tar.zst"
          "{{ .package.name }}" "{{ .package.version }}" "{{ .package.source }}"
          "{{ index .package "runtime-dependencies" }}" "{{ .lfs.jobs }}"
          "{{ .xbee.src }}" "{{ .xbee.out }}" "{{ .xbee.resources }}/build.sh"
