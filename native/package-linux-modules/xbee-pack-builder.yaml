schema-version: 1.0

description: Build the Linux kernel modules package for XBee LFS.

require: ../../build-system

builder:
  - origin: ../package-template
    alias: tools
  - origin: ../final-sources
    alias: sources
  - origin: ../final-system
    alias: system
  - origin: ../package-linux-kernel
    alias: linux-kernel
  - origin: ../package-kmod
    alias: kmod

var:
  xbee:
    version: "6.18.10"
  lfs:
    jobs: 4
    config_revision: 3
  package:
    name: linux-modules
    version: "6.18.10"
    source: linux-6.18.10.tar.xz
    runtime-dependencies: linux-kernel,kmod

build:
  - shell:
      cmd:
        - >-
          bash "/opt/xbee-lfs-native/build-xbpkg.sh"
          "/opt/xbee-lfs-native/sources"
          "/opt/xbee-lfs-native/final-rootfs.tar.zst"
          "{{ .package.name }}" "{{ .package.version }}"
          "{{ .package.source }}" "{{ index .package "runtime-dependencies" }}"
          "{{ .lfs.jobs }}" "{{ .xbee.src }}" "{{ .xbee.out }}"
