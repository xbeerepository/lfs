schema-version: 1.0

description: Package the DocBook XML 4.5 DTD from BLFS 13.0 for XBee LFS.

require: ../../build-system

builder:
  - origin: ../package-template
    alias: tools
  - origin: ../blfs-sources
    alias: sources
  - origin: ../final-system
    alias: system
  - origin: ../package-blfs-libarchive
    alias: blfs-libarchive
  - origin: ../package-blfs-libxml2
    alias: blfs-libxml2

var:
  xbee:
    version: "4.5-2"
  lfs:
    jobs: 4
  package:
    name: docbook-xml
    version: "4.5"
    source: docbook-xml-4.5.zip
    runtime-dependencies: libarchive,libxml2

build:
  - shell:
      cmd:
        - >-
          bash "/opt/xbee-lfs-native/build-xbpkg.sh"
          "/opt/xbee-lfs-native/sources"
          "/opt/xbee-lfs-native/final-rootfs.tar.zst"
          "{{ .package.name }}" "{{ .package.version }}"
          "{{ .package.source }}" "{{ index .package "runtime-dependencies" }}"
          "{{ .lfs.jobs }}" "{{ .xbee.src }}" "{{ .xbee.out }}"
          "{{ .xbee.resources }}/build.sh"
