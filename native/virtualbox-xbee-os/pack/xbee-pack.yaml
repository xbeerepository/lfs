schema-version: 1.0

description: Validate the minimal bootable XBee OS image.

require: ../../xbee-os

provision:
  - shell:
      cmd:
        - test -x /bin/bash
        - test -x /usr/bin/systemctl
        - test -x /usr/sbin/sshd
        - test -x /usr/bin/xbpkg
        - id xbee
